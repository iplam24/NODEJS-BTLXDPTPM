<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sửa TK</title>
    <link rel="stylesheet" href="/css/signup.css">
</head>
<body>
    <div id="modalForm" class="modal">
        <div class="register-container">
            <h2>Sửa tài khoản</h2>
            <form action="/sua-taikhoan" class="signup-form" method="post">
                <label>Tài khoản</label>
                <div class="inputForm">
                    <input type="text" class="input" placeholder="Nhập vào tên tài khoản" name="username" value="<%= userEdit ? userEdit.UserName : '' %>" readonly>>
                </div>
    
                <label>Mật khẩu</label>
                <div class="inputForm">
                    <input type="text" class="input" placeholder="Nhập vào mật khẩu" name="password" value="<%= userEdit ? userEdit.PassWord : '' %>">
                    <span class="togglePassword"></span>
                </div>
    
               
                <label>Họ và tên</label>
                <div class="inputForm">
                    <input type="text" class="input" placeholder="Họ và tên" name="myname" value="<%= userEdit ? userEdit.Full_name : '' %>">
                </div>
    
                <label>Email</label>
                <div class="inputForm">
                    <input type="text" class="input" placeholder="Nhập vào email" name="email" value="<%= userEdit ? userEdit.Email : '' %>">
                </div>
    
                <label>Số điện thoại</label>
                <div class="inputForm">
                    <input type="number" class="input" placeholder="Số điện thoại" name="phone" value="<%= userEdit ? userEdit.Phone : '' %>">
                </div>
    
                <label>Địa chỉ</label>
                <div class="inputForm">
                    <input type="text" class="input" placeholder="Nhập vào địa chỉ" name="address" value="<%= userEdit ? userEdit.Address : '' %>">
                </div>
                <div class="inputForm">
                    <label for="quyen">Chọn quyền:</label>
                    <select id="quyen" name="quyen">
                        <option value="khachhang" <%= userEdit && userEdit.Role === 2 ? 'selected' : '' %>>Khách hàng</option>
                        <option value="nhanvien" <%= userEdit && userEdit.Role === 1 ? 'selected' : '' %>>Nhân viên</option>
                        <option value="nhanvien" <%= userEdit && userEdit.Role === 0 ? 'selected' : '' %>>------</option>
                    </select>
                    
                </div>
                
                <button class="button-submitt">Lưu</button>
    
            
            </form>
        </div>
    </div>
    <script src="/js/signup.js"></script>

</body>
</html>